@model IEnumerable<AutoTest.Models.SubCategory>

@{
    ViewBag.Title = "SubCategory";
}

<h2>SubCategory</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { }, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BusinessEntity.BusinessEntityName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SubCategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BusinessEntity.BusinessEntityName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SubCategoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubCategoryID }, new { @class = "btn btn-warning" })
            @Html.ActionLink("Details", "Details", new { id=item.SubCategoryID }, new { @class = "btn btn-info" })
            @Html.ActionLink("Delete", "Delete", new { id=item.SubCategoryID }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
